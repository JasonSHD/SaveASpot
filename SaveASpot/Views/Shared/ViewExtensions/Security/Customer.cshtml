@using Newtonsoft.Json
@using SaveASpot.Core.Security
@model Customer
<script>
	q(function () {
		q.security.currentCustomer().authenticate(@Html.Raw(JsonConvert.SerializeObject(Model.AsCustomerJson())));
		q.controls.currentCustomer();
	});
</script>
