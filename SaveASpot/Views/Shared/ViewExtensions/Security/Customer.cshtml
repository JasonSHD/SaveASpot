@using Newtonsoft.Json
@using SaveASpot.Core.Security
@model SaveASpot.ViewModels.ViewExtensions.CustomerCartViewModel
<script>
	q(function () {
		q.security.currentUser().fullUser(@Html.Raw(JsonConvert.SerializeObject(Model.Customer.AsCustomerJson())));
		q.controls.currentUser();

		q.cart.currentCart().cart(@Html.Raw(JsonConvert.SerializeObject(Model.Cart.AsCartJson())));
		q.controls.cart();
	});
</script>
