@using Newtonsoft.Json
@using SaveASpot.Core.Security
@model Customer
<script>
	q(function () {
		@Html.Raw("q.security.currentCustomer().authenticate(" + JsonConvert.SerializeObject(Model.AsCustomerJson()) + ");")
	});
</script>
